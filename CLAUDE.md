@AGENTS.md

# CLAUDE.md — PUMP

The repository guide and conventions live in [`AGENTS.md`](AGENTS.md), imported
above. That file is the single source of truth and is shared with opencode,
which reads `AGENTS.md` and can read neither this file nor `@` imports.

**Adding guidance:** put it in `AGENTS.md` (or `.agents/instructions/` and inline
it there), not here.
